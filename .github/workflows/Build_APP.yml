name: Build luci-app-ssr-plus

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialization environment
        run: |
          sudo apt-get update
          # 安装 zstd 用于解压 .zst 文件
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
            gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget zstd

      - name: Download and Setup SDK
        run: |
          SDK_URL="https://downloads.immortalwrt.org/releases/24.10.2/targets/qualcommax/ipq807x/immortalwrt-sdk-24.10.2-qualcommax-ipq807x_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          
          wget -qO sdk.tar.zst "$SDK_URL"
          mkdir openwrt-sdk
          
          # 使用 zstd 解压并流式传输给 tar，同时剥离顶层目录防止 mv 错误
          zstd -d sdk.tar.zst --stdout | tar -x -C openwrt-sdk --strip-components=1

      - name: Update and Install Feeds
        run: |
          cd openwrt-sdk
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Add luci-app-ssr-plus
        run: |
          cd openwrt-sdk/package
          # 克隆包含该插件的仓库
          git clone --depth=1 https://github.com/Valkyrie202604/helloworld.git
          
      - name: Configure and Build
        run: |
          cd openwrt-sdk
          # 产生默认配置
          make defconfig
          # 强制将 luci-app-ssr-plus 设为编译模块 (m)
          echo "CONFIG_PACKAGE_luci-app-ssr-plus=m" >> .config
          # 解决依赖项配置
          make defconfig
          
          # 编译插件本身及其依赖
          make package/helloworld/luci-app-ssr-plus/compile V=s -j$(nproc)

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: luci-app-ssr-plus-ipq807x
          # 路径适配 qualcommax 架构的 bin 目录
          path: openwrt-sdk/bin/targets/qualcommax/ipq807x/packages/*.ipk
